switch to using a raycast to determine which biome is being looked at
    on_entered mostly works but if you try to get it to break its pretty easy to

some of the clipping that happens between multiple mountains